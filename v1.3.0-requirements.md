# v1.3.0 공통 이력서 작성 요구사항

- 작성일: 2026-09-21
- 기준 버전: `v1.2.0`
- 대상 저장소: [JH8459/JH8459.github.io](https://github.com/JH8459/JH8459.github.io)
- 산출물: 기존 포맷을 유지한 3페이지 PDF 이력서 `v1.3.0`
- 성격: 특정 회사 전용이 아닌 범용 백엔드 엔지니어 이력서
- 상태: `feat-resume-v1-3-0` 작업본에 반영 중

## 목표

기존의 범용적인 Node.js 백엔드 엔지니어 포지셔닝을 유지하면서 다음 신호를 `v1.2.0`보다 선명하게 드러낸다.

1. 사용자와 운영자의 요구사항을 데이터 모델·API·도메인 규칙으로 구현한 경험
2. 실패를 전제로 외부 연동과 비동기 이벤트, 배포·관측 구조를 운영한 경험
3. WebSocket·WebRTC 기반 실시간 서비스를 비롯한 구체적인 문제 해결 성과

LG CNS, 메신저 직무 등 특정 지원 대상을 전면에 드러내지 않는다. 고객사 경험은 특정 제품명을 앞세우지 않고 온라인 인적성 검사 운영 경험의 근거로 사용한다.

## 작업 전 확인

1. 대상 저장소의 루트 `AGENTS.md`와 관련 `.codex/rules/` 문서를 먼저 읽는다.
2. 현재 작업 트리와 기존 이력서 소스, PDF 생성 방식, `v1.2.0` 산출물 및 버전 관리 규칙을 확인한다.
3. 사용자가 만든 기존 변경 사항을 덮어쓰거나 되돌리지 않는다.
4. `v1.2.0`은 보존하고 기존 버전 규칙에 따라 `v1.3.0`을 별도 생성한다.
5. 이력서 소스나 PDF 생성 방식이 현재 작업 트리에서 발견되지 않으면 새 생성 시스템을 임의로 만들지 않는다. 확인한 경로와 누락된 입력을 보고하고 중단한다.

`gatsby-meta-config.ts`의 `about.resume`을 이력서 콘텐츠 source-of-truth로 사용하고, `/about`의 브라우저 인쇄 스타일로 3페이지 PDF를 검증한다.

## 포지셔닝

핵심 인상은 다음과 같다.

> Node.js·TypeScript 기반 실서비스를 개발하고, 제품 요구사항의 구조화부터 변경 검증과 장애 대응까지 책임지는 백엔드 엔지니어

다음 원칙을 지킨다.

- 헤드라인과 소개에서 LG CNS, 메신저 직무, 지원 공고를 직접 언급하지 않는다.
- AI 경험은 삭제하지 않지만 대표 포지셔닝으로 앞세우지 않는다.
- 고객사 요구사항 구조화, 실패 대응, 변경 검증, 장애 대응을 중심에 두고 실시간 통신은 경력의 구체적인 성과로 보여준다.
- LG·SK·CJ·두산 등 고객사명은 온라인 인적성 검사 운영 경험의 범위를 보여주는 용도로만 사용한다.
- 직접 메신저를 개발한 것처럼 표현하지 않는다.
- 확인되지 않은 협업 대상, 운영 규모, 성과, 수치를 만들지 않는다.

### 헤드라인 후보

1안:

> 복잡한 제품 요구를 서비스 구조로 풀어내고, 안정적인 운영까지 책임집니다.

2안:

> 제품 요구사항을 서비스 구조로 구체화하고 운영까지 책임지는 백엔드 엔지니어입니다.

`메신저`, `LG`, `대기업`은 헤드라인에 넣지 않는다.

## 소개 문단

`v1.1.0`과 `v1.2.0` 소개의 제품·운영 관점과 실패 대응 표현을 유지하면서 다음 순서로 재구성한다.

1. Node.js·TypeScript 기반 B2B·B2C 서비스 개발·운영
2. 사용자·운영자 요구사항을 데이터 모델과 API, 도메인 규칙으로 구체화한 경험
3. 로그·메트릭·트레이스와 실행 계획에 근거한 성능·장애 개선
4. 외부 연동과 비동기 이벤트의 실패 대응, 서비스 경계와 복구 경로 설계
5. AI 기능 개발은 마지막 보조 문장으로 축약

참고 문장:

> Node.js와 TypeScript를 기반으로 B2B·B2C 서비스를 개발하고 운영해 왔습니다. 사용자와 운영자의 요구사항을 데이터 모델과 API, 도메인 규칙으로 구체화하고, 실패를 전제로 외부 연동과 비동기 흐름을 설계합니다. 운영 지표와 실행 계획을 근거로 병목과 장애를 개선해 안정적으로 운영되는 제품을 만드는 데 집중합니다.

참고 문장을 그대로 복사하지 말고 기존 이력서의 어조와 실제 근거에 맞게 다듬는다. 경력 기간은 실제 기간을 유지한다. 기존 기준에 부합하면 `5년 차`는 사용할 수 있지만 `5년 이상 경력`으로 확대하지 않는다.

## 에이시지알 경력 재구성

프로젝트 기간에는 시작일과 종료일만 표기하고 `3년 3개월` 같은 합산 기간은 노출하지 않는다. 종료일이 최근인 프로젝트부터 다음 순서로 배치한다.

1. B2B 채용·검사 솔루션 백엔드 개발 및 운영 고도화 (`2022.04 - 2025.06`)
2. 온라인 화상 감독 인적성 검사 플랫폼 신규 개발 및 비용 최적화 (`2022.04 - 2024.12`)
3. Electron 기반 부정행위 방지 검사 애플리케이션 개발 및 운영 표준화 (`2023.11 - 2024.05`)

각 프로젝트 안에서는 기존 사실과 수치를 유지하고, 실시간 서비스는 WebSocket과 WebRTC의 역할을 구분해 구체적으로 작성한다. Electron 프로젝트는 듀얼 모니터 감지·프로그램 차단과 자동 업데이트·로그·빌드 검증 성과를 각각 구분해 남긴다.

### 실시간 서비스

다음 사실을 중심으로 `문제 → 판단·설계 → 구현·운영 → 결과` 순서로 작성한다. WebSocket과 WebRTC를 하나의 기술 키워드로 묶지 말고 각각의 역할을 구분한다.

- WebSocket 기반 감독자·응시자 간 실시간 감독 메시지 양방향 전달
- WebRTC P2P 기반 화상 스트림 연결
- 외부 솔루션 uPrism의 기능·운영 통제 한계를 자체 플랫폼으로 전환
- 연간 약 8,000만 원의 라이선스 비용 절감
- 교시 종료 시 녹화 업로드가 집중되며 발생한 서버 부하
- S3 presigned URL 직접 업로드를 이용한 애플리케이션 서버 부하 분리
- winston 로그를 이용한 원인 추적
- Grafana 임계 알림을 통한 조기 감지
- 확인된 경우에만 Lambda·EventBridge 후속 처리와 비용 절감 근거 사용

참고 문장:

> WebSocket으로 감독자·응시자 간 실시간 감독 메시지를 양방향 전달하고, WebRTC P2P로 화상 스트림을 연결해 필요한 감독 흐름을 자체 플랫폼에 구현했습니다. 교시 종료 시 집중되는 녹화 업로드는 S3 직접 업로드로 분리하고, 로그 기반 원인 추적과 Grafana 임계 알림으로 같은 유형의 부하를 조기에 감지할 수 있게 했습니다.

측정 기준이 확인되지 않은 `주 1만 명`, `1인당 약 100MB` 등의 수치는 사용하지 않는다.

### 대기업 고객사 경험

LG Way Fit 제품명은 노출하지 않는다. LG·SK·CJ·두산 고객사 경험은 인적성 검사를 온라인에서 운영할 수 있도록 솔루션을 개발한 맥락에서 함께 표기할 수 있다.

참고 문장:

> LG·SK·CJ·두산 등 대기업 고객사가 인적성 검사를 온라인에서 운영할 수 있도록 채용·검사 솔루션을 개발하고, 고객사마다 다른 검사 콘텐츠·결과·연동 규격을 공통 데이터 모델과 B2B API로 표준화했습니다.

적용 전에 기존 근거나 사용자 확인을 통해 다음 사항을 점검한다.

- 고객사별로 직접 담당한 기능과 API
- 검사 콘텐츠·응답·채점·결과 중 담당 범위
- 고객사 또는 내부 기획·운영 담당자와의 협업 범위
- 운영 규모 또는 측정 가능한 결과

확인되지 않은 사항은 추가하지 않는다. `주도`, `총괄`, `설계 전담` 등의 표현도 근거가 없으면 사용하지 않는다.

## 이력서에서 제외한 활동

MikroORM v7 런타임 회귀 제보는 메인테이너 활동이나 직접 패치 기여가 아닌 단순 이슈 제보로 판단해 `v1.3.0` 이력서에서 제외한다. 관련 기록은 GitHub 이슈·활동 로그로 남긴다.

## 축약과 지면 재배분

새로운 내용을 단순 추가하지 않고 기존 bullet을 교체·통합·축약해 지면을 확보한다.

- 첫 화면의 AI 철학 설명은 삭제하거나 한 문장으로 축약한다.
- Gemini 댓글 검수의 세부 지표는 핵심 성과 한 줄만 남길 수 있다.
- 개인 프로젝트는 핵심 기술 판단과 결과만 남긴다.
- 광고 노출 수집의 세부 구현은 정합성 핵심만 유지하고 변경 검증 근거를 강화한다.
- Electron 프로젝트는 기간과 핵심 구현·운영 성과를 유지하되 중복되는 배경 설명은 줄인다.

## 3페이지 포맷

최종 PDF는 정확히 3페이지를 유지한다.

- 기존 용지 크기, 여백, 타이포그래피, 색상, 섹션 계층을 최대한 유지한다.
- 3페이지를 맞추기 위해 본문 글씨를 과도하게 줄이거나 행간을 비정상적으로 좁히지 않는다.
- 문장이나 bullet이 페이지 경계에서 어색하게 잘리지 않게 한다.
- 링크가 레이아웃을 깨뜨리지 않도록 기존 표기 규칙을 따른다.
- 기존보다 정보 밀도는 높일 수 있지만 가독성은 낮추지 않는다.

## 금지 사항

- LG CNS 지원용이라는 표현
- 메신저 구축·운영 경험이 있다는 주장
- 실제 경력을 `5년 이상`으로 확대
- 확인되지 않은 트래픽·응시자·고객사 규모
- 특정 고객사의 검사 시스템 전체를 설계하거나 총괄했다는 표현
- 근거 없는 `대규모`, `고가용성`, `엔터프라이즈 아키텍처` 등의 수식어
- `v1.2.0` 파일의 덮어쓰기 또는 삭제
- 기존 레이아웃의 전면 재설계

## 검증

기존 저장소가 제공하는 생성·검증 명령을 우선 사용한다.

1. PDF가 정확히 3페이지인지 확인한다.
2. 전체 페이지를 PNG로 렌더링한다.
3. 잘린 문장, 겹침, 여백 붕괴, 깨진 한글, 링크 오버플로가 없는지 시각적으로 확인한다.
4. PDF 텍스트를 추출해 내용 누락과 예상치 못한 중복을 확인한다.
5. `v1.2.0`과 비교해 글자 크기와 가독성이 악화되지 않았는지 확인한다.
6. 변경된 모든 성과와 수치가 기존 근거 또는 위 공개 자료에 연결되는지 확인한다.
7. 저장소의 관련 lint·build·test 명령을 실행한다.
8. `git diff --check`를 실행한다.

문제가 발견되면 수정하고 다시 렌더링한다. 최초 생성 성공만으로 완료 처리하지 않는다.

## 완료 보고

다음을 한국어로 보고한다.

- 수정한 파일
- 생성한 `v1.3.0` PDF 경로
- 3페이지 유지 여부
- 소개와 경력에서 달라진 핵심
- 축약하거나 삭제한 내용
- 제외한 활동과 제외 이유
- 고객사별 역할과 범위 중 확인되지 않아 제외한 내용
- 실행한 검증과 결과
- 남아 있는 사실 확인 항목

커밋, push, PR 생성은 별도 요청이 없으면 수행하지 않는다.

## 완료 기준

- 특정 회사용이라는 인상 없이 Node.js·TypeScript 실서비스 개발·운영 역량이 유지된다.
- 범용적인 제품 개발·운영 역량 안에서 WebSocket·WebRTC 경험이 구체적인 핵심 경력으로 읽힌다.
- WebSocket의 양방향 메시지 전달과 WebRTC의 P2P 화상 연결 역할이 구분된다.
- 특정 제품명 없이 대기업 고객사의 온라인 인적성 검사 운영 경험이 구체적으로 드러난다.
- MikroORM 이슈 제보는 이력서 외부 활동에서 제외된다.
- 확인되지 않은 역할·성과·규모가 추가되지 않는다.
- PDF가 기존 디자인과 가독성을 유지한 정확히 3페이지다.
